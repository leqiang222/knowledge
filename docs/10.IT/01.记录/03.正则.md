---
title: 正则
date: 2025-07-22 16:53:37
permalink: /pages/109adf/
---
### 元字符 

- `.` 匹配除换行符以外的任意字符
- `\b` 匹配单词的开始或结束，也就是单词的分界处
- `\d` 纯单个数字
- `\w` 匹配字母或数字或下划线或汉字
- `\s` 匹配任意的空白符，包括空格，制表符(Tab)，换行符，中文全角空格等
- `^` 匹配字符串的开始
- `$` 匹配字符串的结束

### 重复

- `*` 它指定*前边的内容可以连续重复使用任意次以使整个表达式得到匹配
  - 因此，.*连在一起就意味着任意数量的不包含换行的字符
- `+` 重复一次或更多次
- `?` 重复零次或一次
- `{n}` 重复n次
  - \d{8}：表示纯数字重复8次
- `{n,}` 重复n次或更多次
- `{n,m}` 重复n到m次

### 字符类

- `[xxx]` 匹配xxx
  - [0-9]代表一位数字，和\d是完全一致的
  - [a-z0-9A-Z_]也完全等同于\w（如果只考虑英文的话）

### 分枝条件

- `|` 
  + 0\d{2}-\d{8}|0\d{3}-\d{7}这个表达式能匹配两种以连字号分隔的电话号码：一种是三位区号，8位本地号(如010-12345678)，一种是4位区号，7位本地号(0376-2233445)。

### 分组

### 反义

| \W       | 匹配任意不是字母，数字，下划线，汉字的字符 |
| -------- | --------------------- |
| \S       | 匹配任意不是空白符的字符          |
| \D       | 匹配任意非数字的字符            |
| \B       | 匹配不是单词开头或结束的位置        |
| [^x]     | 匹配除了x以外的任意字符          |
| [^aeiou] | 匹配除了aeiou这几个字母以外的任意字符 |
